# План: гнучкий матчинг карток

Документ для наступної сесії. Описує систему матчингу карток, усунення деструктивного авто-масштабування, збір аналітики відмов та масштабування під Taker і Maker режими.

---

## 0. Статус (оновлено 09.08.2026)

| Розділ | Стан |
|---|---|
| Етап 1 — недеструктивний автосейл | ✅ зроблено |
| 7.7 — дефолти в одну точку + міграція | ✅ зроблено (`config/card_limits.py`) |
| 7.2 / 7.3 — довідник банків | ✅ зроблено (`config/banks.BANK_PROFILES`) |
| 7.4 — нічне вікно, tier у скорингу | ✅ частково: нічне вікно й tier діють; `business_days_only` лише зберігається |
| 7.6 — `ThresholdFee` + ставки з профілю (п. 1–2) | ✅ зроблено |
| 7.6 — калькулятор у тейкері (п. 3) | ✅ зроблено |
| 7.6 — комісія у виборі картки (п. 4) | ⬜ **розблоковано етапом 3** — у кошику тепер бувають картки різних банків, тож комісія переказу нарешті їх розрізняє |
| Етап 2 — причини відмов назовні | ✅ зроблено, статистика збирається |
| Етап 3 — кошики й міжбанк | ✅ під фічами `/features` → 💳 КАРТКИ |
| Етап 4 — налаштування користувача | ✅ крім `inter_bank` (замок до етапу 3) |
| Етап 5 — попередження в майстрі, `/checkup` | ✅ зроблено |
| Алерт про 80% ліміту картки | ✅ зроблено (працює і в тейкері) |
| Етап 5.5 — міжбіржова комісія при продажу | ✅ зроблено (лоти для неї не потрібні) |
| Етап 5.5 — собівартість (лоти) | ⬜ лише для чистого профіту, чекає на історію угод |
| Етап 6 — мейкер | ⬜ відкладено користувачем 09.08.2026 («мейкер потім») |

Уточнення до 7.7 за фактом: `DEFAULT` у DDL стояв на таблиці
`user_bank_limits`, а не `cards` (у `cards` ліміти лежать у
`limits_override_json`, де записані лише свідомо задані ключі). Замість
окремої колонки `limits_source` роль позначки грає сам `NULL`: порожнє
поле означає «користувач не задавав», і тоді діє довідник.

---

## 1. Симптом, з якого все почалось

```
├ Капітал: 21297.8 (Авто · monobank) (на картках 31 123 у різних банках) ₴
├ Режим сканування: TAKER_BUY

📉 Баланс картки зменшився! Суму купівлі авто-масштабовано
📊 700.00 USDT (~31,045 ₴) ➔ 480.22 USDT (~21,298 ₴)
```

Баланси: Monobank 21 298, ПУМБ 5 005, Sense 4 820. Разом 31 123 ₴.
Користувач виставив 700 USDT. Бот зробив 480.

---

## 2. Три корені, які накладаються

### 2.1. Деструктивний запис
`trigger_buy_autoscale_check` і гілка авто-масштабування в циклі викликають `db.update_taker_buy_amount(user_id, calc_usdt)` — тобто **перезаписують поле, яке ввів користувач**. Введені 700 USDT більше не існують: у `scanner_users.taker_buy_amount` лежить 480.22.

Навіть після поповнення картки 700 не повернуться — `auto_scale_up` порахує нову цифру від поточних балансів, а не відновить бажану.

**Це найдорожчий із трьох дефектів і найдешевший у виправленні.**
Місця: `core/engine/taker_scanner.py` — `trigger_buy_autoscale_check()` і блок `buy_bal_mode == "AUTO_SCALE"` всередині `find_orders_for_user()`.

### 2.2. Поточна стеля движка: один банк
`CardMatchingEngine.run(user_id, bank, amount, direction)` досі приймає **один** банк (`core/engine/card_matching_engine.py:22`). Спліт між картками він уміє — але лише в межах цього одного банку.

Тому `get_user_auto_capital(allowed_banks=...)` і зараз чесно повертає `max` по банку (21.2k Mono), а не суму 31k: показувати 31к, коли зібрати їх движок сьогодні не вміє, було б брехнею. Оновлювати цифру капіталу до загальної суми має сенс **тільки разом із розширенням движка на декілька банків**.

### 2.3. Модель «одна цифра»
Весь конвеєр оперує скаляром `capital`. Ордер проходив або не проходив одну перевірку. Але в P2P **багато карток — це норма**, і кожна має свій баланс, свої ліміти IN/OUT, свій прогрів і свій cooldown.

---

## 3. Правила матчингу та сплітів

Сценарій **«ПУМБ 5к + Сенс 5к = 10к під ордер 10к»** проходить **НЕ безумовно**, а тільки за двох умов одночасно:
1. Мерчант в ордері вказав прийом на **обидва банки** (ПАТ ПУМБ і Сенс Банк).
2. Користувач у налаштуваннях увімкнув **`[Спліт між банками]`** (`inter_bank`).

> Якщо користувач обрав режим `[Спліт: тільки в межах одного банку]` (`intra_bank`), цей ордер на 10k повинен бути відхилений. Це **коректна поведінка**, а не баг.

---

## 4. Сценарій 20+ карток (Дроп-карти)

При 20 картках (наприклад, 1 основна + 3 дроп-карти на банку) комбінаторика $O(2^N)$ дає 1000+ варіантів на ордер і блокує сканер.

**Рішення:** Жадібний алгоритм відбору $O(N \log N)$:
1. Сортувати картки за придатністю (`score` = прогрів, залишок ліміту, баланс).
2. Набирати згори, поки не покриємо `target_amount` або `min_limit` (до max_cards_per_trade).
3. Окремий облік напрямку:
   - `buy` (купівля USDT = відправка фіату) → перевіряємо `OUT` ліміти.
   - `sell` (продаж USDT = прийом фіату) → перевіряємо `IN` ліміти.

---

## 5. Строгий порядок етапів

Порядок етапів принциповий. Кожен наступний спирається на емпіричні дані з попереднього:

### Етап 1 — Перестати псувати введене (Недеструктивний автосейл) ✅
- `taker_buy_amount` більше НЕ перезаписується в БД.
- Бажані 700 USDT зберігаються назавжди, а `effective_max_usdt` обчислюється на льоту для фільтрації.

**Як зроблено.** `core/engine/buy_budget.resolve_buy_budget()` — чиста
функція «бажана сума + доступні гроші + курс → з чим іти в цей ордер».
Нічого не зберігає. Наслідки, які виявились по дорозі:

* роль «перенести змасштабовану суму далі» раніше грав сам запис у БД:
  фінальний прохід матчингу перечитував `taker_buy_amount`. Тепер масштаб
  живе в `scaled_usdt_by_order` у межах одного проходу;
* тротлінг алерта про масштабування став обов'язковим: раніше повторів не
  було лише тому, що запис у БД «розв'язував» ситуацію;
* `buy_auto_scale_up` більше не відновлює суму — вона й не втрачається.
  Прапорець тепер керує повідомленням «баланс відновлено»;
* кнопка «⚡ Увімкнути авто-масштабування» в алерті про нестачу коштів не
  мала декоратора `@router.callback_query` — натискання не робило нічого;
* тайт-толерантність у фінальному матчингу читала неіснуюче поле
  `match_res.available_balance` через `getattr(..., 0)`, тож не спрацьовувала
  жодного разу. Додано `CardMatchResult.available_uah`;
* `get_user_auto_capital` викидала картку з `max_tx_per_day = -1`
  («♾️ Ігнорувати»), бо `tx_count >= -1` завжди істина.

### Етап 2 — Причини відмов назовні та збір статистики ✅
`CardMatchResult.rejection_report` вже існує в движку, але не виводиться в бот/API.
- Довести причини відмов до Telegram-алертів та API `/taker/orders` (напр. *«Мін. 30 000 ₴, на Монобанку 21 298 ₴ — не вистачає 8 702 ₴»*).
- **Зібрати статистику причин за 1 тиждень.**
- *Обґрунтування:* Якщо 90% відмов мають одну просту причину, складна система з 25 кошиками буде надлишковою. Будувати Етап 3 наосліп — це вгадування.

**Як зроблено.**

* `core/engine/rejection_codes.py` — стабільні коди плюс формулювання з
  цифрами. Код потрібен, щоб «не вистачає балансу» і «не вистачає 8 702 ₴»
  рахувались як одна причина; текст — щоб людина зрозуміла, що робити;
* `TakerScanner.scan()` повертає `(orders, rejections)`;
  `find_orders_for_user()` лишився тонкою обгорткою — на нього спираються
  чотири викликачі;
* таблиця `card_rejection_log` з ключем `(день, режим, ордер, код)`. Дедуп
  тут не оптимізація: сканер проходить стакан щохвилини, і без нього
  статистика показувала б не «які причини переважають», а «скільки кіл
  встиг зробити сканер». Прунінг — два тижні, з `DBMaintenanceTask`;
* `/card_rejections` у боті, `rejected[]` у `/taker/orders`,
  `/taker/rejections` для тижневого зрізу;
* дайджест «ордери є, але жоден не проходить по картках» на порожньому
  проході — не частіше разу на 6 годин на кожну причину. Досі порожній
  результат виглядав однаково і коли ринку немає, і коли ордерів десяток,
  але картки їх не тягнуть.

**Доповнення 09.08.2026: автоскейл робив статистику невидимою.**

Перший же запуск `/card_rejections` на живих даних показав порожньо — при
тому, що стеля одного банку впиралась щогодини. Причина: коли автоскейл
ужимає обсяг під наявне, ордер **проходить**, і в журнал не потрапляє
нічого. Тобто рівно та ситуація, заради вимірювання якої й будувався етап 2,
не давала жодного запису.

Тому додано **спостереження** (`rejection_codes.OBSERVATION_CODES`):
`volume_scaled_down` пишеться, коли ордер дійшов до користувача, але меншим.
Це не відмова, і в частку відмов воно не входить — інакше вийшло б «90%
відмов» по ордерах, які людина насправді отримала. У звіті це окрема секція
«Пройшли, але меншим обсягом» із сумою недобору.

Спостереження пишеться лише по ордерах, які реально дійшли до користувача:
масштабування ордера, що потім відсіявся з іншої причини, не є фактом «стеля
коштувала нам обсягу».

**Коли дивитись на дані.** Через тиждень роботи: `/card_rejections`.

| Що переважає | Що це означає |
|---|---|
| `volume_scaled_down` із великим недобором | стеля одного банку коштує обсягу — етап 3 виправданий |
| `no_cards_for_bank` | питання не в кошиках, а в тому, які банки підключені |
| `insufficient_balance` | грошей просто мало; кошики не допоможуть |
| порожньо | усе сходиться, етап 3 не потрібен |

**Важлива межа етапу 3, яку варто тримати в голові:** міжбанківський спліт
допомагає лише тоді, коли мерчант приймає ДВА банки користувача одночасно.
Якщо він приймає тільки Monobank, гроші в ПУМБ не врятує жоден движок. Тож
перед етапом 3 треба дивитись не лише на частоту, а й на перетин банків
мерчанта з банками карток.

### Етап 3 — Кошики та міжбанківський движок ✅ (за фічею)
- Перетин трьох множин: `банки мерчанта ∩ банки фільтра ∩ банки живих карток`.
- Жадібний міжбанківський набір карток.
- Оновлення `get_user_auto_capital()` для показу сумарного капіталу.

**Зроблено 09.08.2026 під експериментальними фічами** (`/features` → 💳
КАРТКИ), за прямим побажанням користувача. З вимкненими фічами поведінка
рівно та, що була, — це під тестом.

| Фіча | Що робить |
|---|---|
| `inter_bank_matching` | дозволяє збирати суму з карток різних банків; розблоковує режим «Спліт: між банками» |
| `ignore_merchant_bank_filter` | знімає вимогу, щоб банк був у оголошенні мерчанта; маршрут повертається з позначкою «узгодити в чаті» |

**Ключове рішення користувача:** фільтр банків мерчанта — не закон. Мерчанти
часто перелічують не все, що приймають, тож ордер варто показати й під
картки, яких немає в оголошенні, а питання «чи можна з ПУМБ?» коштує одного
повідомлення. Тому маршрут не блокується — він **позначається**: у
картковому блоці, ПЕРЕД кнопкою «взяти», стоїть «❓ Спитайте в чаті перед
оплатою: мерчант не вказував ПУМБ».

**Вибір маршруту** (6.3): спершу одна картка, потім кошик у межах одного
банку, і лише потім міжбанківський. Одиночний вибір і кошик рахують той
самий `_score_card()` — дві копії ваг розійшлись би, і одиночний вибір почав
би віддавати перевагу не тій картці, що набір.

**Приклад із розмови, який тепер працює як треба.** Ордер на 5 000 ₴: у ПУМБ
рівно 5 000, у Сенсі 4 800, у Монобанку 21 000. Обирається **ПУМБ** — одна
транзакція закриває картку, а великий залишок Монобанку лишається під
більший ордер. Раніше ПУМБ навіть не розглядався: мерчант вказав Monobank.

**Кошик — жадібний**, $O(N \log N)$, сортування за доступною сумою вниз:
мета — мінімум переказів, бо кожна зайва нога це ще один застосунок під
15-хвилинний таймер. Скоринг іде другим ключем. Комбінаторний перебір
свідомо не використано (6.3, 4).

**Маршрутизація живе в одному місці** — `core/engine/card_routing.
resolve_route()`. Її питають і сканер, і будівник алерта: дві копії
розійшлись би, і сканер вважав би ордер придатним, а алерт малював би під
нього інші картки. Розбіжність було б видно лише в момент угоди.

---

**Хвіст, який знайшов користувач того ж дня: інтерфейс не знав про фічу.**

Увімкнувши обидва перемикачі, користувач і далі бачив на дашборді
«зібрати їх в одну угоду движок поки не вміє — гроші в різних банках».
Движок на той момент уже вмів. Не знали два місця:

* `readiness.check_taker_readiness()` — текст писався на етапі 5, тобто ДО
  етапу 3, і стверджував обмеження беззастережно. Після вмикання фічі він
  казав людині неправду про її ж налаштування;
* `get_user_capital_breakdown()` — повертала `usable` як максимум по одному
  банку **завжди**. Саме звідси бралися ті 21 298 ₴ у попередженні.

Виправлено: `usable` дорівнює сумі, коли кошики увімкнені (а `best_bank`
порожній — маршрут із кількох банків, і одна назва вводила б в оману);
блокер «жоден з обраних банків не має картки» перестав бути блокером, коли
фільтр банків мерчанта вимкнено; а коли фіча вимкнена, текст тепер каже, ДЕ
її ввімкнути, замість просто «не вміє».

**Урок ширший за цей баг.** Пояснення в інтерфейсі — така сама залежність
від поведінки движка, як і код, тільки компілятор про неї не нагадає.
Змінюючи те, що движок уміє, треба шукати не лише виклики функцій, а й
**фрази, які описують стару межу**. Тут це коштувало того, що найпомітніша
фіча виглядала зламаною в момент, коли вона вперше запрацювала.

### Етап 4 — Налаштування користувача ✅
- `[Спліт карток]`: Вимкнено / У межах банку / Між банками.
- `[Максимум карток на угоду]`: 1 / 2 / 3.
- `[Відображення відхилених ордерів]`: Приховати / Сірим з причиною.

**Як зроблено.** Усі три — в меню налаштувань карткового модуля
(`disp:cycle:*`), у `user_card_settings` і в API `/user/card-display`.
Константи режимів — у `config/card_limits`, бо їх читають і сховище, і
движок, і клавіатури; тримати їх у движку означало б, що шар даних
імпортує шар логіки.

Що знайшлось по дорозі:

* **`max_cards_per_order` існував лише на папері.** Поле було в схемі з
  дефолтом 3, `_generate_splits` його читав, `card_notifier.py:423` радив
  користувачу «перевірте max_cards_per_order у налаштуваннях» — а меню не
  було, і `update_user_card_settings` це поле навіть не згадував. Восьмий
  почерк мовчазного зламу: *налаштування, на яке посилається текст для
  користувача, але змінити його нізвідки*;
* **`inter_bank` не зберігається взагалі.** Движок уміє збирати суму лише
  в межах банку; записати вибір, який нічого не змінює, — та сама мовчазна
  брехня. Режим видно в меню із замком і поясненням, API віддає 400.
  Знімається однією правкою `SPLIT_MODES_AVAILABLE` разом з етапом 3;
* **`0 or 3`.** Перша версія валідації затирала свідомий нуль трійкою
  замість того, щоб затиснути в діапазон. Спіймано тестом.

Дефолти підібрані так, щоб нічого не змінити наявним користувачам:
`card_split_mode='intra_bank'` — це поточна поведінка движка,
`show_rejected_orders='with_reason'` — бо мовчазне зникнення ордера і було
тим дефектом, який лікував етап 2.

### Етап 5 — Попередження в майстрі та Онбординг ✅

`core/engine/readiness.py` — одна функція `check_taker_readiness()`, яку
кличуть майстер, швидкий старт, екран пресету й команда `/checkup`. Копії в
кожному з них розійшлись би: цей проєкт уже п'ять разів на цьому обпікся
(див. пам'ять `silent-failure-patterns`, пункт 5).

Порядок перевірок навмисно повторює порядок движка, щоб майстер називав ту
саму причину, яку потім назве `/card_rejections`. Розбіжність «майстер
сказав ок, а сканер мовчить» була б гіршою за відсутність перевірки.

Три рівні: `blocker` — алертів не буде взагалі; `warning` — будуть, але не
такі, як людина очікує; `note` — просто варто знати.

| Що ловимо | Рівень |
|---|---|
| Модуль карток увімкнено для одиночних режимів, карток немає | 🛑 |
| Жоден з обраних банків не має активної картки | 🛑 |
| Частина обраних банків без карток | ⚠️ |
| Обсяг купівлі більший за капітал у одному банку | ⚠️ (💡 з авто-масштабуванням) |
| Місячний оборот банку вибрано на 80%+ | ⚠️ |
| Спліт вимкнено / модуль карток не бере участі | 💡 |

Дві деталі, які варто тримати в голові:

* **«є всього» проти «піде в одну угоду».** Попередження називає обидві
  цифри, коли вони різні: саме ця розбіжність колись показувала 31 123 ₴ у
  меню й 21 298 ₴ у масштабуванні;
* **перевірка не блокує запуск.** Виняток усередині — не привід не
  запустити режим; `readiness_block()` у такому разі просто повертає
  порожній рядок.

### 6.7. Вікно мерчанта проти можливостей карток (09.08.2026)

Знайдено під час прогону сценаріїв користувача, і це виявився найдорожчий
дефект із трьох перевірених.

**Симптом.** Мерчант приймає від 30 000 ₴. На картках одного банку 21 298 ₴.
Автоскейл ужимав обсяг до 21 298 і відправляв алерт — на угоду, яку мерчант
не візьме за жодних обставин. Дзеркальний випадок: користувач хоче 700 USDT
(~31 000 ₴), стеля мерчанта 7 000 ₴ — картки перевірялись під 31 000, яких
однаково не прийняли б.

**Корінь.** Дві сторони рахувались незалежно: движок відповідав «скільки
можуть мої картки», сканер — «що приймає мерчант», і після масштабування
ніхто не звіряв перетин.

**Рішення** (обидва — у `taker_scanner`, і в пре-фільтрі, і в фінальному
проході, бо сума там рахується наново):

* сума затискається стелею мерчанта: більше `max_limit` в цю угоду не піде;
* якщо ужата сума нижча за `min_limit` — ордер відхиляється з кодом
  `below_merchant_min` і цифрою недобору, а не показується.

**Що це дало для сценарію 1.** З увімкненими обома фічами етапу 3 той самий
ордер стає робочим: 31 122 ₴ по трьох банках перекривають мінімум мерчанта.
Сам по собі `inter_bank_matching` не рятує — мерчант назвав один банк, а
кошик без `ignore_merchant_bank_filter` за межі заявленого не виходить.

**Чому НЕ вводимо мін/макс ліміти на кожну картку** (обговорювалось із
користувачем і відхилено ним же). Обмеження, яке хочеться цим виразити —
«не витрачай велику картку на дрібний ордер» — це властивість МАРШРУТУ, а не
картки, і воно вже є в `_score_card()`: бонус за те, що сума закриває картку
майже повністю. Персональні мін/макс на картку дали б три нові колізії —
з мінімумом мерчанта, з налаштуванням швидкості й між собою — і жодної
нової можливості.

### Етап 5.5 — Собівартість продажу: звідки взявся USDT (ідея, 09.08.2026)

Пропозиція користувача. У TAKER_SELL система рахує виручку, але не знає
**собівартості**: скільки USDT і за якою ціною закуплено, а головне — **на
якій біржі**. Без цього міжбіржова комісія за переказ криптовалюти в
розрахунок не входить, хоча вона реальна: продавати на Bybit те, що
куплено на Binance, дорожче, ніж продавати там же.

Механізм для комісії мережі вже написаний — `NetworkFeeEngine.
get_optimal_network(buy_ex, sell_ex)` у `core/utils/fees.py`. Бракує рівно
одного: знання, звідки прийшов лот.

**Що робить задачу нетривіальною** (з формулювання користувача):

1. **Поле необов'язкове.** Людина може нічого не вводити, і тоді все має
   працювати як зараз. Тобто це не нова обов'язкова сходинка майстра, а
   збагачення — і жодна перевірка не сміє відкидати ордер через порожню
   собівартість. Порівняй із трактуванням порожніх умов у 6.2: невідоме
   лишається невідомим;
2. **Закуп буває з кількох бірж.** Велика сума збирається частинами: 300
   USDT на Binance, 400 на Bybit. Отже це не поле «біржа закупу», а
   **список лотів**: `(біржа, кількість, ціна, дата)`;
3. **Продаж не збігається з лотом.** Продаємо 500 з 700 куплених — треба
   вирішити, з яких лотів списувати. Найпростіше й найзрозуміліше для
   людини — FIFO, з можливістю обрати лот руками;
4. **Комісія залежить від пари бірж.** Для маршруту «лот з Binance →
   продаж на Bybit» комісія одна, «Bybit → Bybit» — нуль. При продажу з
   кількох лотів комісія рахується покомпонентно й підсумовується.

**Що це дає:** чистий профіт замість валової виручки, і — головне —
підказку «цей ордер вигідніший, бо продаєш там же, де купував».

**Межі:** ручне ведення лотів швидко набридає. Реальна цінність з'явиться,
коли лоти заповнюватимуться самі — з історії угод бірж, а це вже вимагає
підключених API-ключів і звірки. Тримати як окремий етап, не змішувати з
матчингом карток.

---

**Зроблено 09.08.2026 — і задача виявилась меншою, ніж здавалась.**

Перевірка перед кодуванням показала, що **для комісії лоти не потрібні
взагалі**. Мережевий переказ коштує однаково незалежно від того, за скільки
куплені монети; знати треба рівно одне — **де вони зараз**. А це вже
віддають підключені ключі: `/balance` ходить по `get_balance()` кожної
біржі. Тобто найдорожча частина задачі — ручне ведення лотів — до питання
користувача стосунку не мала.

Задача розпалась на два незалежні шари:

| Шар | Для чого | Введення |
|---|---|---|
| **Де лежить USDT** ✅ | міжбіржова комісія | нуль, автоматично з ключів |
| Скільки коштував | чистий профіт замість виручки | лоти, вручну або з історії угод |

`core/engine/usdt_inventory.py` — знімок балансів із TTL-кешем (сканер
ходить по стакану щохвилини, а `get_balance()` це мережевий запит) плюс
чиста функція `plan_transfer()`: скільки вже на місці, звідки везти решту,
якою мережею й за скільки. Донори беруться за спаданням балансу — комісія
мережі фіксована за переказ, тож два перекази по 100 USDT дорожчі за один
на 200.

В алерті TAKER_SELL:

```
⛓ Треба перевести: 400.00 USDT з Binance (TON)
   └ Комісія мережі: 0.0100 USDT (~0 ₴)
```

…або `⛓ USDT уже на Bybit — переказ не потрібен`.

**Поправка того ж дня: гаманець важить не менше за біржу.** Користувач
вказав на те, чого код не знав: після купівлі на P2P монети падають на
**спот**, а щоб продати їх на P2P, вони мають бути на **фандингу**.
`get_balance()` у всіх клієнтах зливає обидва гаманці в одне число — тобто
перша версія рахувала спотові USDT як готові до продажу й казала «переказ не
потрібен» там, де насправді потрібен внутрішній переказ.

Тоді ж додався третій стан — **Earn**: клієнти не бачили його взагалі, тобто
монети в гнучкому Earn були для бота невидимі. Додано `get_earn_balance()`
у Binance, Bybit і OKX (лише FLEXIBLE — locked достроково не забрати, тож
видавати його за доступний під таймер угоди означало б обіцяти гроші, яких
не буде).

Сходинка готовності — це не три однакові кошики, а три різні відстані до
угоди:

| Стан | Що треба зробити | Ціна |
|---|---|---|
| `funding` | нічого | — |
| `spot` | переказ усередині біржі | безкоштовно, миттєво |
| `earn` | викуп | безкоштовно, не завжди миттєво |
| інша біржа | вивід мережею | комісія мережі |

Порядок відповідно: фандинг → спот → Earn → чужа біржа.

Окремо є прапорець `earn_known`. «В Earn нуль» і «Earn ми не бачимо» — різні
речі: у другому випадку фраза «не вистачає USDT» видавала б межу власної
видимості за факт про чужий гаманець. Тому текст стає «не вистачає за тим,
що видно боту *(Earn і стейкінг бот не бачить — перевірте там)*».

Ендпоінти Earn написані за документацією й **не перевірялись на живому
акаунті** — ключів під рукою не було. Помилка тут не мовчазна: `earn_known`
лишиться `False`, і повідомлення це скаже.

Три межі, витримані свідомо:

* **невідомі баланси ≠ нуль.** Ключів немає або біржа не відповіла —
  мовчимо. Нуль вів би до висновку «треба переказувати», а невідоме не
  веде ні до якого (той самий принцип, що й з порожніми умовами в 6.2);
* **немає спільної мережі — так і кажемо.** `NetworkFeeEngine` віддає
  `("UNKNOWN", 999.0)`; подавати 999 USDT як комісію було б брехнею, тому
  такий маршрут окремо позначається як «доведеться вручну»;
* **недобір по всіх біржах показується**, а не ховається.

**Що лишилось із 5.5:** шар собівартості (лоти) — для чистого профіту. За
власним висновком плану, робити його руками сенсу мало; братись варто разом
із читанням історії угод бірж.

### Алерт про наближення до ліміту картки ✅ (09.08.2026)

Формально це третій пункт етапу 6, але до мейкера він не прив'язаний:
йдеться про картку, тож працює і в тейкері.

Перевірка на 80% у проєкті вже була — в дашборді карток. Тобто спрацьовувала
лише якщо людина сама відкриє меню: дізнатись, що картка ось-ось упреться в
стелю, можна було, тільки здогадавшись піти подивитись.

`core/engine/limit_watch.py` перевіряє поріг у момент запису транзакції —
там же, де з тих самих міркувань уже живе авто-кулдаун на 95%. Додано
**місячний** ліміт: у довіднику він менший за добовий (Monobank 100к/міс
проти 150к/добу), тож упирається першим, а попереджав про нього досі ніхто.
Показується те попередження, чия межа зупинить наступну угоду першою, а не
те, чий відсоток більший. Одне на картку/напрямок/період на добу.

### Етап 6 — Інтеграція з Maker режимом (Маркер на майбутнє)

> **Відкладено користувачем 09.08.2026: «мейкер потім».** Роботу зупинено
> свідомо, а не забуто. Лишились два пункти: підказка карток при створенні
> й репрайсингу оголошення (мейкерські движки з картковим модулем не
> пов'язані взагалі) і моніторинг чату з 6.5 — той самий, про який план
> окремо попереджає, що за обсягом він співмірний з усім рештою.

**Умова, яка може змінити оцінку 6.5 (09.08.2026).** Користувач іде на
статус мерчанта: на Binance 7 000 з 10 000 обороту і 48 з 50 угод, далі —
перенесення статистики на OKX. Це важливо, бо **в OKX є офіційний P2P API
саме для мерчантів**, і саме його бракує сьогодні: `get_my_p2p_orders` для
OKX — заглушка з коментарем «OKX не віддає приватні P2P дані без
спеціальних дозволів». Ті дозволи й дає мерчантський статус.

Тобто 6.5 («моніторинг чату — це polling до API кожної біржі, різні
формати й авторизація») оцінювався для світу, де приватного P2P API немає
взагалі. З мерчантським доступом на OKX задача з «співмірної з усім
рештою плану» стає задачею під одну біржу з документованим API. Переоцінити
обсяг варто саме тоді, коли статус з'явиться, — не раніше, бо доти немає
чого читати.

**І в Bybit він теж є.** Екран дозволів ключа має розділ «Торгівля
фіатними валютами» з правами окремо на перегляд ордерів і заявок та на їх
зміну («заплатіть або схваліть ордери», «додайте або змініть заявки»).
Таких дозволів не існувало б без ендпоінтів.

У `bybit_account.py` при цьому роками стояло «Bybit не віддає P2P дані
через звичайний API» — тобто перша спроба дістала 404, і причину записали
як властивість біржі замість «ми стукаємо не туди». Це окремий почерк
помилки, вартий уваги: **невдала спроба, записана як факт про світ**,
закриває напрямок назавжди, тоді як «ми не знайшли шлях» лишає його
відкритим. Докстрінги виправлені; реалізації немає, бо не було на чому
перевірити.
- Картковий модуль повинен підтримувати TAKER і MAKER режими.
- Для Maker режимів (`AdRepricer`, `MakerAdMonitor`): при створенні/репрайсингу оголошення бот підказує оптимальні карти для прийому (IN-ліміти на `SELL` оголошеннях) та відправки (OUT-ліміти на `BUY` оголошеннях).
- Автоматичний алерт при досягненні 80% добового IN/OUT ліміту на картці мейкера.

---

## 6. Крайові випадки

### 6.1. Асиметрія Taker Buy і Taker Sell

**Buy**: фіат відправляю я, тож спліт — цілком моє рішення. Обмежують лише
умови мерчанта й таймер угоди.

**Sell**: фіат відправляє мерчант **мені**. Попросити його розбити платіж
на дві-три мої картки — не «умова», а прохання, і зазвичай це відмова або
зайва тяганина в чаті.

**Наслідок для етапу 3:** для `sell` кошики з кількох карток майже
безглузді. Там працюють одиночні картки, і стелю задає не сума балансів, а
`daily_in` / `monthly_in` найкращої картки. Половину комбінаторики для
цього напрямку можна не будувати взагалі — замість неї потрібен нормальний
вибір ОДНІЄЇ картки під ордер.

### 6.2. «Без третіх осіб» — тільки в ролі тейкера

Умови чужого оголошення діють на мене, лише коли я **тейкер**. Коли я
мейкер, умови мого ордера пишу я сам, і написане в чужих оголошеннях до
мене не стосується.

* **Taker**: якщо в terms мерчанта є заборона третіх осіб — не змішувати
  власні картки з дроп-картками. Окремий аналізатор писати не треба:
  `risk_engine` уже проганяє terms через regex і LLM, звідти й брати сигнал.
* **Maker**: обмеження задає власна конфігурація оголошення. Реальні
  домовленості («можна з картки дружини?», «можна двома переказами?»)
  живуть у чаті угоди — див. 6.5.

**Коли умов не видно** (`UNKNOWN:TERMS:EMPTY`, немає сесії тощо) —
розділяємо дві різні речі:

* **показ ордера** — показуємо з ворнінгом: «за параметрами підходить, але
  умов і відгуків дістати не вдалось». Рішення за людиною;
* **автоматичне складання маршруту** зі змішаними картками — це вже дія
  бота. Тут за замовчуванням пропонуємо маршрут без змішування, а змішаний
  показуємо другим варіантом із позначкою.

Різниця в тому, хто ризикує наосліп: у першому випадку людина бачить і
вирішує, у другому бот вирішив за неї, спираючись на те, чого не знає.

### 6.3. Кількість переказів і таймер

Мерчанти часто пишуть «тільки одним платежем». Плюс таймер угоди зазвичай
15 хвилин, і три перекази через три банківські застосунки — це реальний
ризик апеляції, а не просто «повільніше».

Ранжування маршрутів: `1 картка > N карток одного банку > N карток різних
банків`. Робити не лише сортуванням, а й жорстким обмеженням
(`max_cards_per_trade`).

### 6.4. Банківські групи зі спільною ліцензією

Ліміти рахуються **на картку**, не на особу — місячні ліміти на фізособу
настільки великі, що впертись у них не вийде.

Але частина брендів працює на ліцензії банку-партнера, і тоді картки
різних брендів ділять один ліміт. Користувач бачить два різні банки й
вважає, що має два незалежні ліміти — а їх один.

**Підтверджені групи** (джерело — розділ 7):

| Група | Банки | Що спільне |
|---|---|---|
| `tascombank` | Izibank (553) + Таскомбанк | ліцензія, фінмон |
| `vostok` | БВР + Банк Восток | ліцензія, фінмон |

Здогад про `izibank` справдився, але партнер — **Таскомбанк**, не Восток.

Важливо: спільний тут не лише ліміт, а й **фінансовий моніторинг**. Тобто
група — це ще й канал поширення проблеми: блок на одному банку тягне
другий. Для матчингу це означає, що після спрацювання фінмону варто
виводити з обігу всю групу, а не одну картку.

**Рішення:** у `config/banks.Bank` додати необовʼязкове `license_group`.
Порожнє — банк сам по собі (поточна поведінка, нічого не змінюється).
Заповнене — картки всіх банків групи рахують ліміти спільно. Список
дописується з практики без переробки логіки.

### 6.5. Моніторинг чату угод — окрема вага

Розпізнавання домовленостей у чаті («можна з картки третьої особи?»,
«можна двома переказами?») виглядає як «підключити наявні LLM-воркери», але
насправді це polling до API кожної біржі, різні формати й авторизація, плюс
реакція в реальному часі з таймером 15 хвилин.

За обсягом це співмірне з усім рештою плану. Тримати маркером у Maker-етапі,
але без ілюзії, що воно майже готове.

### 6.6. Pre-filtering

Перетин банків мерчанта з банками користувача — **найперша** перевірка, до
будь-яких звернень у базу. Без цього кожен ордер у стакані тягне ліміти й
транзакції карток, і цикл сканера просідає.

---

## 7. Довідник банків замість спільних дефолтів

### 7.1. У чому проблема

`get_card_effective_limits` і `CardMatchingEngine` дають **однакові
дефолти всім банкам**:

```python
daily_out_max: 150_000    monthly_out_max: 400_000
max_single_tx_out: 29_999  max_tx_per_day: 15
```

Зіставлення з практикою (джерело — 7.2) показує, що вони не просто
неточні, а ведуть у зворотний бік від безпеки:

| Банк | Безпечно / міс | Транзакцій | Наш дефолт дозволяє |
|---|---|---|---|
| Izibank | 60к | **2–3** | 15 транзакцій, 400к |
| А-Банк | 60к | 3–5 | 15, 400к |
| Таскомбанк | 50к | 3–5 | 15, 400к |
| Грант | 50к | 3–5 | 15, 400к |
| Monobank | 100к | 10–15 | 15, 400к |

Бот сьогодні спокійно розпише 15 транзакцій на Izibank, де безпечно 2–3, і
вважатиме доступними 400к там, де верхня межа 60–100к. Це не помилка
округлення — це маршрут, що веде до блокування картки.

**Тому дефолти з довідника треба заводити разом з етапом 1**, а не
відкладати на пізніші етапи: етап 1 лише перестає псувати введену суму,
а неправильні ліміти псують саме рішення.

### 7.2. Що заводимо в `config/banks.Bank`

Зараз `Bank` знає `internal_code`, `name`, `exchange_codes`. Потрібен
профіль:

| Поле | Призначення |
|---|---|
| `tier` | 1 / 2 / 3 — вага в `_score_and_pick` |
| `safe_monthly_uah` | рекомендована місячна межа |
| `max_monthly_uah` | межа, вище якої ризик блоку різко зростає |
| `safe_tx_per_day` | скільки переказів на добу не привертає уваги |
| `single_tx_limit_uah` | стеля одного переказу (`None` = без ліміту) |
| `p2p_fee` | комісія переказу: поріг + відсоток + фікс |
| `business_days_only` | не відправляє у вихідні |
| `night_window` | нічні обмеження (сума / заборона) |
| `license_group` | див. 6.4 |
| `termination_fee_pct` | комісія при розриві контракту |
| `third_party_friendly` | чи годиться для роботи з чужими картками |

Ключове: значення з профілю — **дефолт**, який користувач перекриває в
налаштуваннях картки. Заповнене поле картки завжди сильніше за довідник,
інакше зламаються вже налаштовані картки.

### 7.3. Таблиця

Джерело — операційна зведенка з профільного каналу (08.04.2026) плюс
власні дані користувача. Прочерк — даних немає, **не вигадувати**;
дозаповнити з першоджерела перед кодуванням.

**Tier 1**

| Банк | Реком./міс | Tx/день | 1 переказ | Комісія P2P | Примітки |
|---|---|---|---|---|---|
| Monobank | 100к (макс 150к) | 10–15 | без ліміту | 0% по Україні | часті фінмони; 2+ чарджбеки → перевірка → розрив |
| ПриватБанк | 100к (макс 150к) | до 20 | 29 999 | 0.5% між банками | |
| ПУМБ | 100к, безпечно до 70к | — | — | — | дані користувача; решту дозаповнити |
| Глобус | 100к | 5–10 | 24 999 | до 10 переказів / 50к — 0%, далі 1% | **тільки робочі дні**; між банками 1 tx/день |

**Tier 2**

| Банк | Реком./міс | Tx/день | 1 переказ | Комісія P2P | Примітки |
|---|---|---|---|---|---|
| Ощадбанк | 80к | не критично | 29 999 | 1% + 5₴ | 22:00–07:00 не більше 5к |
| KredoBank | 80к | 5–7 | 24 999 | 0.7% + 2.5₴ | |
| Sense | 70к (макс 100к) | 5–10 | 29 999 | до 20к — 0%, далі 1% + 5₴ | IBAN 24/7 |
| А-Банк | 60к (макс 100к) | 3–5 | 29 999 | до 100к — 0%, далі 2% | розрив −30% |
| Izibank | 60к (макс 100к) | **2–3** | 29 999 | до 100к / 20 переказів — 0%, далі 2% + 5₴ | ліцензія Таскомбанку; розрив −20% |
| Таскомбанк | 50к | 3–5 | 24 999 | 0.5% + 10₴ | розрив −20% |
| БВР | 40к | 5–7 | 29 999 | перші 5 — 0%, далі 0.5% | **не відправляє у вихідні**; ліцензія Восток; розрив −30% |

**Винятки на стелю одного переказу** (решта — 29 999):

* Кредит Дніпро — 99 999
* Глобус, KredoBank, Таскомбанк, Банк Львів — 24 999
* Грант — 20 000 (найнижча)
* Monobank, ОТП — без ліміту

**Тільки робочі дні** (IBAN не піде у вихідні): Глобус, Кредит Дніпро,
Procredit, Рада, Cominbank, Укрсиббанк, Unex, Грант, Південний,
CreditAgricole.

**Загальний орієнтир джерела:** 50 000 ₴ на місяць на картку — рівень, на
якому ймовірність блоку мінімальна. Це консервативніше за всі рядки вище
й годиться як глобальний дефолт для банку, якого немає в таблиці.

### 7.4. Три фактори, яких у плані не було

**Робочі дні й години проти таймера угоди.** Десяток банків відправляє
IBAN лише в робочі дні; Ощадбанк уночі обмежений 5к; CreditAgricole
працює 10:00–16:00. При таймері 15 хвилин це означає, що в суботу ввечері
частина карток непридатна для тейкер-угоди — і зараз бот про це не знає,
тож пропонує маршрут, який фізично не виконається.

Матчинг має враховувати **час у момент пошуку**, а не лише баланси.

> **Уточнення від користувача (08.08.2026).** `business_days_only` сам по
> собі нічого не вирішує: обмеження стосується IBAN, а картка на картку в
> тих самих банках іде щодня. Тому вихідні виводять картку з гри **лише
> тоді, коли в умовах мерчанта є згадка про IBAN**
> (`core/engine/terms_signals.mentions_iban`). Порожні умови — це
> «невідомо», а не «так»: глушити картки на здогадці означало б втрачати
> придатні ордери. Нічне вікно застосовується безумовно — то ліміт самого
> переказу.

**Комісія переказу їсть спред.** А-Банк 2% після 100к, Izibank 2% + 5₴,
ПриватБанк 0.5% між банками. При спреді 0.5–1% комісія здатна з'їсти весь
профіт. Механізм для цього вже написаний — `core/utils/fees.py`, деталі
в 7.6.

**Tier як вага скорингу.** `_score_and_pick` зараз зважує прогрів,
свіжість і залишок ліміту. Tier лягає туди природно: за інших рівних
віддавати перевагу картці банку вищого tier.

### 7.5. Що з цього поза матчингом

Не тягнути в матчинг, але зберегти:

* **комісія при розриві контракту** (А-Банк 30%, БВР 30%, Izibank 20%,
  Таскомбанк 20%) — попередження в картці банку, не фактор вибору;
* **прогрів** — довідник підтверджує поточну логіку `cold_card_limit`:
  1–2 тижні звичайного користування, у перший місяць не більше ~50к;
* **треті особи** — «для роботи з чужими картками лише лояльні банки»
  прив'язується до `third_party_friendly` і працює в парі з 6.2.

### 7.6. Куди саме лягають комісії: `core/utils/fees.py`

Калькулятор комісій уже існує, і його форма — рівно та, якої потребує
довідник:

```
BaseFee ← PercentFee | FixedFee | NetworkFee
FeeCalculator.calculate_net(amount, usdt_price) → (net, total, [FeeResult])
get_calculator(buy_bank, sell_bank, buy_ex, sell_ex)
```

`FeeResult` несе ще й `description` — тобто розбивка комісій уже готова до
показу користувачеві. Архітектуру чіпати не треба, потрібні дані й один
новий тип комісії.

**Що в ньому не так сьогодні.**

*Покрито 5 банків із ~30.* Явні гілки є лише для ПриватБанку (`14`) і
Sense (`328`), плюс спільна «крос-банк 0.5%» для Mono / А-Банк / Izi
(`43`, `48`, `553`). Для решти комісія = **0**. Ощадбанк (1% + 5₴),
KredoBank (0.7% + 2.5₴), Таскомбанк (0.5% + 10₴), Глобус (1% після 10
переказів) система вважає безкоштовними.

*Пороги ігноруються, хоча в коментарях описані.* Біля Sense написано «поза
лімітами пакету», але `SENSE_PERCENT` + `SENSE_FIXED` додаються
беззастережно. Переказ на 10к, де до 20к безкоштовно, отримує 1% + 5₴ —
комісія **завищена**.

*Ставки розійшлися з практикою в обидва боки:*

| Банк | У коді | Довідник | Наслідок |
|---|---|---|---|
| А-Банк | 0.5% завжди | 0% до 100к, далі **2%** | занижено вчетверо після порогу |
| Izibank | 0.5% завжди | 0% до 100к / 20 переказів, далі 2% + 5₴ | те саме |
| Monobank | 0.5% крос-банк | **0%** по Україні | зайва комісія |
| ПриватБанк | 0.5% завжди | 0% всередині, 0.5% між банками | завищено для Mono→Mono |

**Головне: тейкер не рахує комісій узагалі.** `get_calculator`
викликається рівно з одного місця — `core/engine/cross_matcher.py:59`,
тобто **лише спред-режим**. А банківський переказ фіату відбувається саме
в тейкерських режимах. Там комісія завжди нуль.

**Що робити (у порядку користі):**

1. **`ThresholdFee`** — новий клас поруч із наявними: безкоштовно до
   `threshold`, далі `pct` + `fixed`. Це закриває більшість рядків
   довідника, бо майже всі банки саме так і тарифікують.
2. **Перенести ставки з констант у профіль банку** (`Bank.p2p_fee`), щоб
   `get_calculator` більше не був `if buy_bank == "14"`. Заодно зникає
   ще одна захардкожена мапа банківських кодів.
3. **Підключити калькулятор до тейкера** — і показувати в алерті чистий
   спред після комісії конкретної картки, а не «до».
4. **Врахувати при виборі картки**: за інших рівних вигідніша та, чий
   переказ дешевший. Це вже вхід у `_score_and_pick`.

Пункти 1–3 корисні самі по собі, без матчингу. Пункт 4 має сенс лише
після нього.

---

**Зроблено (09.08.2026): пункти 1–3.**

Пункт 3 живе в `bot/taker_builder._transfer_fee()`: в алерті TAKER_BUY
з'явився рядок «Комісія переказу» і курс після неї, у зведеному списку —
компактне «З комісією», бо саме там ордери стоять поруч і порівнюються за
ціною, а найдешевший за курсом може виявитись не найдешевшим після комісії.

Два уточнення, які випливли з коду:

* **У тейкері переказ завжди внутрішньобанківський.** Движок добирає картку
  рівно того банку, який приймає мерчант (`card_engine.run(bank=...)`), тож
  комісії «між банками» — ПриватБанк 0.5%, Monobank — тут не виникають
  узагалі. Це не спрощення, а наслідок того, як влаштований матчинг;
* **Сума береться з обсягу користувача, а не з мінімалки ордера** (із
  затиском у ліміти мерчанта). Комісія з порогом залежить від суми прямо:
  рахувати від `min_limit` означало б систематично її занижувати.

**Пункт 4 був безпредметний, а тепер ні.** Коли це писалось, `_score_and_pick`
отримував картки ОДНОГО банку, комісія в них була однакова, і доданок
«вигідніша та, чий переказ дешевший» не розрізняв би нічого. Етап 3 це
змінив: у кошику тепер бувають картки різних банків, а комісії в них різні —
Monobank 0%, Ощадбанк 1% + 5 ₴, А-Банк 2% після порогу. Тобто вибір між
ними нарешті має ціну, і `_score_card()` про неї не знає.

Робити разом із перевіркою на живих даних: за яких сум різниця комісій
перевищує решту ваг скорингу (прогрів важить 100, tier 15–45), бо інакше
доданок або нічого не змінить, або перебиватиме прогрів — а це гірше.

**Свідомо НЕ зроблено: фільтрація за курсом із комісією.** Користувач ставить
«макс. ціна 44.50»; з комісією Ощадбанку ефективний курс 44.82. Відкидати
такий ордер означало б мовчки звузити фільтр, який людина задала сама. Зараз
комісія показується, рішення за людиною — а перемикач «фільтрувати за
курсом із комісією» варто обговорити окремо.

### 7.7. Пастка: дефолти захардкожені в семи місцях

Під час перевірки з'ясувалось, що змінити «спільний дефолт» в одному місці
не вийде. Той самий словник `150000 / 400000 / 29999 / 15` лежить у:

| Файл | Де |
|---|---|
| `core/storage/base_db.py` | 557–563 — **`DEFAULT` у DDL таблиці `cards`** |
| `core/storage/card_repo.py` | 139–142, плюс inline у 388, 699–701 |
| `core/engine/card_matching_engine.py` | 56–59, плюс inline у 89, 93, 97, 134 |
| `bot/card_notifier.py` | 51–54, 168–171, 200–203, 312–315, 542–545 |
| `bot/handlers/cards.py` | 977–980, плюс inline у 135–136, 889 |

Це шосте повторення патерну «скопійована константа, копії якої розійшлись»
(див. пам'ять `silent-failure-patterns`, пункт 5).

Два практичні наслідки:

1. **Спершу звести дефолти в одну точку**, і лише потім заводити довідник.
   Інакше профіль банку діятиме через раз — залежно від того, чий
   `.get(..., 400000.0)` спрацював останнім.
2. **`base_db.py` — не просто копія.** `DEFAULT` у DDL означає, що значення
   вже **записані в рядки БД** для всіх існуючих карток. Змінити константу
   в Python недостатньо: у базі й далі лежатимуть 400 000. Потрібна
   міграція, і в ній треба відрізнити «користувач свідомо поставив
   400 000» від «це дефолт, якого ніхто не чіпав» — інакше міграція
   затре ручні налаштування.

   Найпростіший чесний варіант: додати `limits_source` ('default' /
   'user') і мігрувати лише рядки з `'default'`.

---

## 8. Орієнтири в коді

| Що | Де |
|---|---|
| Перезапис бажаної суми | `core/engine/taker_scanner.py` → `trigger_buy_autoscale_check`, блок `AUTO_SCALE` |
| Матчинг і спліти | `core/engine/card_matching_engine.py` |
| Капітал | `core/storage/card_repo.py` → `get_user_auto_capital`, `get_user_capital_breakdown` |
| Налаштування | `bot/handlers/filters.py` → `on_filters_menu` |
| Мейкер-репрайсер | `core/engine/ad_repricer.py` & `maker_ad_monitor.py` |
| Реєстр банків | `config/banks.py` → `BANKS`, `normalize_bank` |
| Спільні дефолти лімітів (виправити першими) | див. 7.7 — сім місць, з них `base_db.py` DDL |
| Скоринг картки | `core/engine/card_matching_engine.py` → `_score_and_pick` |
| Комісії переказів | `core/utils/fees.py`; єдиний споживач — `core/engine/cross_matcher.py:59` |
| Аналіз terms | `core/engine/risk_engine.py` (regex + LLM, звідти брати сигнал про третіх осіб) |
| Прапорець «власна / дроп» | `cards.is_own`, `cards.category` (self / relative / friend / drop) |
